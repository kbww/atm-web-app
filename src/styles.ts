import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      main: "#800000",
    },
    background: {
      default: "#ffffff",
    },
  },
});

export default theme;
